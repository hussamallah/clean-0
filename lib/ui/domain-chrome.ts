import type { DomainKey } from "@/lib/bigfive/constants";

/** Rim / glow accents aligned with `DetailedResults` DOMAIN_META. */
export const DOMAIN_CHROME: Record<DomainKey, string> = {
  O: "#fbbf24",
  C: "#3b82f6",
  E: "#a855f7",
  A: "#10b981",
  N: "#ef4444",
};
